import CircularProgress, { CircularProgressProps } from '@mui/material/CircularProgress';

export const AppCircularProgress = (props: CircularProgressProps) => <CircularProgress sx={{ display: 'flex' }} {...props} />;
