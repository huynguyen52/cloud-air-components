export enum SortOrder {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum TableId {
  PERMISSION = 'permission',
  ROLE = 'role',
  USER = 'user',
  WORKFLOW = 'workflow',
  COURSE = 'course',
  TRAINER = 'trainer',
  WORKFLOW_LINK = 'workflowLink',
  COURSE_TYPE = 'courseType',
  MOF_EXAM_INFORMATION = 'mofExamInformation',
  COURSE_PARTICIPANT = 'courseParticipant',
  MOF_RESULT_PREVIEW = 'mofResultPreview',
}

export enum AlertType {
  SUCCESS = 'success',
  WARNING = 'warning',
  ERROR = 'error',
  INFO = 'info',
  LOADING = 'loading'
}

export enum ApproveStatus {
  APPROVE = 'Approve',
  REJECT = 'Reject',
}

export enum NodeLevelCode {
  CHANNEL = 'Channel',
  REGION = 'Region',
}